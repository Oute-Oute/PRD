<form method="post" action="{{ path('app_human_resource_category_delete', {'id': human_resource_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ human_resource_category.id) }}">
    <button class="btn-delete btn-secondary">Supprimer</button>
</form>
