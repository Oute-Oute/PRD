<form method="post" action="{{ path('app_material_resource_delete', {'id': material_resource.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ material_resource.id) }}">
    <button class="btn-delete btn-secondary">Delete</button>
</form>
