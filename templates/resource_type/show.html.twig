{% extends 'base.html.twig' %}

{% block title %}ResourceType{% endblock %}

{% block body %}
    <h1>ResourceType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resource_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ resource_type.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ resource_type.category }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ resource_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_resource_type_index') }}">back to list</a>

    <a href="{{ path('app_resource_type_edit', {'id': resource_type.id}) }}">edit</a>

    {{ include('resource_type/_delete_form.html.twig') }}
{% endblock %}
