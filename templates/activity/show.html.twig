{% extends 'base.html.twig' %}
{% block stylesheets %}
                <link rel="stylesheet" href="{{ asset('CSS/Global/ShowCrud.css') }}">
        {% endblock %}
{% block title %}Activité{% endblock %}

{% block body %}
<div id ="show-user">
    <h1>Informations de l'activité {{ activity.id }}</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ activity.name }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ activity.duration }}</td>
            </tr>
        </tbody>
    </table>

   <button class = "btn-consult btn-secondary" onclick="window.location.href='{{ path('app_activity_index') }}'">Consulter les activités</a>

    <button class = "btn-edit btn-secondary" onclick="window.location.href='{{ path('app_activity_edit', {'id': activity.id}) }}'">Éditer</a>

 {{ include('activity/_delete_form.html.twig') }}
{% endblock %}
