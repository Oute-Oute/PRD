{% extends 'base.html.twig' %}

{% block title %}MaterialResourceCategory{% endblock %}

{% block body %}
    <h1>MaterialResourceCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ material_resource_category.id }}</td>
            </tr>
            <tr>
                <th>Categoryname</th>
                <td>{{ material_resource_category.categoryname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_material_resource_category_index') }}">back to list</a>

    <a href="{{ path('app_material_resource_category_edit', {'id': material_resource_category.id}) }}">edit</a>

    {{ include('material_resource_category/_delete_form.html.twig') }}
{% endblock %}
