{% extends 'base.html.twig' %}
{% block stylesheets %}
                <link rel="stylesheet" href="{{ asset('CSS/Global/ShowCrud.css') }}">
        {% endblock %}
{% block title %}Type de circuit{% endblock %}
{% block body %}
<div id ="show-circuit-type">
    <h1>Informations du type de circuit {{ circuit_type.name }}</h1>
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circuit_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ circuit_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <button class = "btn-consult btn-secondary" onclick="window.location.href='{{ path('app_circuit_type_index') }}'">Consulter les types de circuit</a>

    <button class = "btn-edit btn-secondary" onclick="window.location.href='{{ path('app_circuit_type_edit', {'id': circuit_type.id}) }}'">Éditer</a>

    {{ include('circuit_type/_delete_form.html.twig') }}
</div>
{% endblock %}
