<form method="post" action="{{ path('app_circuit_delete', {'id': pathway.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pathway.id) }}">
    <button type ="submit" class="btn-delete btn-secondary">Supprimer</button>
</form>
